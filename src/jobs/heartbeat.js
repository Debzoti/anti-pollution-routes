// heartbeat.js — placeholder for Step 4 (scoring + notification trigger)
// This will run every 15 minutes to score routes and trigger alerts.

export async function runHeartbeat() {
  // TODO Step 4: call scoring engine, trigger notifier
  console.log('[heartbeat] tick — scoring not yet implemented');
}
